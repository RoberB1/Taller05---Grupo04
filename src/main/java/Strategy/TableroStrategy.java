package Strategy;

public class TableroStrategy implements StrategyView {
    @Override
    public void moldearVista(){
        // Se aplica logica para que la vista se configure como tablero
    }
}
